"""Test package marker.

Pytest may run with import modes that require test helper imports to resolve via a
real Python package (e.g. `from tests.e2e.e2e_test_utils import ...`).

This file intentionally contains no runtime logic.
"""
