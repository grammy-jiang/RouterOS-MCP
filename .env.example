# Add to .gitignore if committing actual secrets
# Otherwise use environment variables

ROUTEROS_MCP_ENVIRONMENT=lab
ROUTEROS_MCP_DEBUG=true
ROUTEROS_MCP_LOG_LEVEL=DEBUG
ROUTEROS_MCP_DATABASE_URL=sqlite:///./routeros_mcp.db

# For production, set these via secure secret management
# ROUTEROS_MCP_ENCRYPTION_KEY=your-base64-encoded-key
# ROUTEROS_MCP_OIDC_CLIENT_SECRET=your-oidc-secret
